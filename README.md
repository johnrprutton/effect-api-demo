# Effect API Demo

Quick look at how to create HTTP APIs using Effect

## Get started

- Install `pnpm`

  ```
  npm i -g pnpm@10.26.1
  ```

- Install deps

  ```
  pnpm i
  ```

- Build `.ts` to `.js`

  ```
  pnpm build
  ```

- Run the demo

  ```
  pnpm start
  ```
